import React from "react"
import Layout from "../../components/layout/layout"

export default () => (
  <Layout path={"/mrsi/faq"} hideSideNav centerContent MaxWidth={700}>
    This is the faq
  </Layout>
)
